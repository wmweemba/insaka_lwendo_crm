# Copy to .env.local for dev, or set directly in Coolify env vars for deploy.
# Never commit a file with real values — see .gitignore / .dockerignore.

DATABASE_URL="postgresql://user:password@localhost:5432/insaka_lwendo_crm"

# Better Auth
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# Inbound integration secrets (added as each ingest endpoint is built — see docs/planning/02-capture-and-integrations.md)
# BAZABOOKS_WEBHOOK_SECRET=""
# CHAMA360_WEBHOOK_SECRET=""
# ZEROCLAW_API_KEY=""
